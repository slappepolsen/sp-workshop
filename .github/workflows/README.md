# Workflows

- **`build.yml`** is the active CI for validation, frozen builds, and release artifacts.
- **`build.yml.disabled`** is kept only as a historical reference, see header comment why it must stay disabled. Do not re-enable or copy from it without auditing for stale paths.
